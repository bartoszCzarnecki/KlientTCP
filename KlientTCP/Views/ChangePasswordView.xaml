<UserControl x:Class="KlientTCP.Views.ChangePasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KlientTCP.Views"
             mc:Ignorable="d" 
             FontSize="20"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock FontSize="40" Margin="5" HorizontalAlignment="Center">Change Password</TextBlock>
        <TextBlock Margin="5">New Password</TextBlock>
        <PasswordBox x:Name="Password" Margin="5"/>
        <TextBlock Margin="5">Confirm Password</TextBlock>
        <PasswordBox x:Name="Password2" Margin="5"/>
        <Button x:Name="HandleButtonClick" Margin="5">Submit</Button>
        <TextBlock Text="{Binding Path=Error, Mode=OneWay}" Foreground="Red" FontSize="16" HorizontalAlignment="Center"/>
    </StackPanel>
</UserControl>
